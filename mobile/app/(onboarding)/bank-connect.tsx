import { View, Text } from "react-native";
import { router } from "expo-router";

export default function BankConnectScreen() {
    return (
        <View>
            <Text>Bank Connect</Text>
        </View>
    );
}